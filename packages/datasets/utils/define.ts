import type { ChatDataSet, ChatSession } from '@yunlefun/chat-ui'

export function defineSession(session: ChatSession) {
  return session
}

export function defineDataSet(dataSet: ChatDataSet) {
  return dataSet
}
